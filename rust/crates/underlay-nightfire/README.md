# underlay-nightfire

Deprecated crate-name facade for [`nightfire`](https://github.com/inflatable-cookie/nightfire).

Existing code can keep importing `underlay_nightfire`; every public item is
re-exported from the released standalone crate. New code should depend on
`nightfire` directly.
