# Required keys for startup. List keys only; do not store values here.
# Static authority (`underlay-env-authority`) checks membership in env-manifest.
# Live presence stays with scripts/check-env-manifest.sh against the process env.
DATABASE_URL
AUTH_JWT_PRIVATE_KEY
AUTH_JWT_PUBLIC_KEY
AUTH_OAUTH_SECRET_KEY
