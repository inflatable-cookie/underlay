# Declared env surface for startup and local validation.
# Keep only true secrets and real runtime-env keys here.
# Static authority (`underlay-env-authority`) reads this key list only.
DATABASE_URL
ENVIRONMENT_NAME
HOST
PORT
PUBLIC_HOST
AUTH_JWT_PRIVATE_KEY
AUTH_JWT_PUBLIC_KEY
AUTH_OAUTH_SECRET_KEY
