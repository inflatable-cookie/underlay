#!/usr/bin/env bun

/**
 * Published CLI entry for consumer workspace-shape conformance.
 * Imports the public `@inflatable-cookie/underlay/tools/workspace-shape` surface.
 */

import { runWorkspaceShapeCli } from '../src/tools/workspace-shape.js';

runWorkspaceShapeCli();
