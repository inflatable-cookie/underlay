// Render-only registrations for Nightfire blocks. This is imported by
// the renderer so that student/public views can use Nightfire without
// pulling in any editor components.

import "./markup/render";
import "./summary/render";
