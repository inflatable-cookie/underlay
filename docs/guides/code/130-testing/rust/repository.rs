// Minimal placeholder so `code/130-testing/` has a Rust example.
// See `docs/guides/quickstart/130-testing.md` for fuller patterns.

#[cfg(test)]
mod tests {
    #[test]
    fn example_unit_test() {
        assert_eq!(2 + 2, 4);
    }
}
