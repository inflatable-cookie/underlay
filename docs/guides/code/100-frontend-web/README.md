# 100 Frontend Web Code

These files are retained as integration-oriented snippets only.

They are not the canonical home for shared UI implementation. Use:

- Poodle guides for generic UI composition
- ACME reference files for real visible implementations

See:

- `Poodle Svelte Developer Guide`
- `List And Filter Recipes`
- `Dialog And Detail Recipes`
- the ACME front dashboard and project-detail routes in the separate
  `underlay-reference` repository

The snippets in this folder should only demonstrate how frontend-web structure
and retained Underlay integration fit together.

Current retained files:

- `app.d.ts`
- `hooks.server.ts`
- `lib/api/client.ts`
- `routes/+layout.server.ts`
