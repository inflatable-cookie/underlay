# 110 Admin Code

These files are retained as integration-oriented snippets only.

They are not the canonical home for shared UI implementation. Use:

- Poodle guides for generic admin shell, page, form, and dialog composition
- ACME admin routes and layouts for the real visible reference surface

See:

- `Admin App Shell Recipes`
- `Admin Feature Delivery Recipes`
- `Page Shell And Admin Recipes`
- the ACME admin app-layout, projects list, and project-detail routes in the
  separate `underlay-reference` repository

The snippets in this folder should only demonstrate how retained Underlay
runtime/client concerns fit into an admin app structure.

Current retained files:

- `+layout.svelte`
- `app.d.ts`
- `hooks.server.ts`
- `lib/api/client.ts`
- `lib/ui/AdminNavList.svelte`
- `lib/ui/AdminUserMenu.svelte`
- `routes/+layout.server.ts`
