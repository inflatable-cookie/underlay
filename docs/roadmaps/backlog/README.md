# Backlog

Backlog files hold unscheduled Underlay work that is worth preserving but not
currently committed to an active roadmap.

Promotion path: when a backlog item is ready, compile a bounded roadmap under the
active generation (`g09` as of 2026-08-17) or record explicit defer criteria
here if it stays out of scope.
