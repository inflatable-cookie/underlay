# Backlog

Backlog files hold unscheduled Underlay work that is worth preserving but not
currently committed to an active roadmap.

Promotion path: when a backlog item is ready, compile a bounded numbered roadmap
under the active generation. No generation is active after `g09` closure, so a
new generation must be opened explicitly before promotion. Otherwise record
defer criteria here.
