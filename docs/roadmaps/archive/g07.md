# g07 - Runtime, Workflow, And Doctor Warning Boundary Hardening

Status: archived
Kind: roll-up
Generation: `g07` (49 task files, `g07.001` onward)
Succeeded by: `g08`

## Intent and boundary

`g07` hardened the retained TypeScript runtime, workflow pattern,
template, and support-layer surface after the Rust reference-grade
reset: ownership inventories and audits, import-guidance cleanup,
pattern-helper diet, orchestration-dedup decisions, seam audits,
testing and guardrail reinforcement, residual Rust public-policy
retirement, and the doctor-warning cleanup lane. It was explicitly not
another broad UI rewrite.

## Shipped outcomes

- Runtime and workflow: TS runtime/workflow ownership inventory,
  runtime subpath public-surface audit, import-guidance cleanup,
  duplicated auth-aware fetch orchestration decision, list/pagination/
  reorder/template seam audits, relation selector boundary audit.
- Patterns and templates: pattern-helper ownership diet with its
  decision artifact, template seam posture.
- Support layer: TS testing and guardrail gap inventory, public-surface
  test and guardrail reinforcement.
- Rust residue: direct config-construction retirement behind
  builders/accessors.
- Test god-file splits: TypeScript, slugify, forms, i18n, SvelteKit,
  and CSP suites.
- Closeout: OAuth Rust file cleanup and the `g07.037` doctor-warning
  closeout.

## Canonical destinations

TS runtime authority lives in Contract `090`, patterns in Contract
`100`, templates in Contracts `110`/`111`, and tooling in Contract
`120`. Nothing in this roll-up is executable.

## Migrations, limits, and retained risks

Consumer import changes were deliberate, classified, and validated per
lane. No package or crate ownership boundaries changed.

## Deferred items

None. The generation is complete after the `g07.037` doctor-warning
closeout.

## Selected references

- Closing lane: `g07.037` (full text in git history before
  compaction).
- Successor: `archive/g08.md`.
