# g03 - Template System

Status: archived
Kind: roll-up
Generation: `g03` (`g03.001`–`g03.051`)
Succeeded by: `g04` (parallel contract-coverage thread) and `g05`

## Intent and boundary

`g03` replaced hand-rolled admin page composition with reusable
higher-order Svelte templates: the `EntityList`/`EntityDetail` section
components and page shells, `EntityForm` surfaces, the Acme-admin and
Dairy proof lines, the Underlay template skill, and the consumer rollout
plan. It closed at `g03.051` with the Dairy `SpaFormShell`
normalization line, stopping at the real evidence boundary.

## Shipped outcomes

- Template primitives: `EntityList`, `EntityListPage`,
  `EntityDetail`, `EntityDetailPage`, `EntityForm`, `EntityFormPage`.
- Proof lines: Acme-admin list/detail/form proofs and the Dairy
  validation, exam, schedule, area, outcome, module, and syllabus
  form/copy-move proofs.
- Supporting work: generation rollover posture, docs reorganization
  (`docs/usage/` skeleton), TS structure refactor
  (`ts/src/templates/`), template skill, and consumer rollout plan.

## Canonical destinations

Template authority lives in `docs/contracts/110-*`,
`docs/contracts/111-*`, and `docs/usage/templates/`. Contract
`110` cites the `g03` rollout evidence (rollover, Acme list proof,
Acme detail proof, Dairy validation, entity forms); those citations now
resolve here.

## Migrations, limits, and retained risks

None retained. Later detail-page and tab-list convergence continued in
`g05`.

## Deferred items

None. All `g03` roadmaps are complete.

## Selected references

- Closing lane: `g03.051` (full text in git history before compaction).
- Successors: `archive/g04.md`, `archive/g05.md`.
