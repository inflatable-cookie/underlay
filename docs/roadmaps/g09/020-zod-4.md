# g09.020 - zod 4 (underlay root only)

Status: complete
Completed: 2026-08-04
Owner: repo maintainers

## Purpose

zod 3.25 → 4.4, used only in underlay's root package. Smallest of the
deferred majors; needs its own review of zod 4's API changes (error
customization, `.default()`/`.catch()` semantics, `z.string().email()`
deprecations).

## Evidence

- JS dependency survey 2026-08-03 (zod 3.25.76 → 4.4.3 in underlay root)

## Planned Changes

- [x] Find every zod usage in underlay ts/src (imports, schema
  definitions, `.parse`/`.safeParse`, error handling).
- [x] Review zod 4 migration guide; adjust schemas/error handling.
- [x] `effigy validate` green (tsc + vitest suites).

## Consumer Upgrade Impact

Impact class: `additive` (underlay-internal; no consumer uses zod).

## Validation

- [x] `effigy validate` green

## Stop Conditions

None expected.

## Completion Notes

Completed 2026-08-04 (zod 4.4.3). Usage was confined to `patterns/validated-form.svelte.ts` (+ test): two API breaks fixed — `z.AnyZodObject` → `z.ZodObject`, `z.SafeParseReturnType<I,O>` → `z.ZodSafeParseResult<O>`; `error.issues` path was already v4-compatible. Peer range `^3.25.0 || ^4.0.0`. tsc clean, 758 unit tests green, svelte-check 0/0.

## Next Task

Generation closeout or `g10` scoping (maintainer direction).
