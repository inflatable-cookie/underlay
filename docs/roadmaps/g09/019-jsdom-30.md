# g09.019 - jsdom 30

Status: complete
Completed: 2026-08-04
Owner: repo maintainers

## Purpose

Test-environment major 28 → 30 for the packages using jsdom (underlay
component tests, acme-admin/cp-admin/dairy/froyo). CSS parsing and API
changes can break component tests in ways that look like app bugs — needs
a test-suite-first rollout.

## Evidence

- JS dependency survey 2026-08-03 (jsdom 28.1.0 → 30.x in
  acme-admin, cp-admin, dairy, froyo; underlay root)

## Planned Changes

- [x] Review the jsdom 29/30 changelogs for behavior changes (CSSOM,
  URL/navigation, canvas, matchMedia, layout APIs).
- [x] underlay first: bump, run the full component suite (45 tests) +
  hygiene check; fix environment-level fallout in the shared setup file,
  not per-test.
- [x] Consumers: bump per package, run each vitest suite; A/B any new
  failure against jsdom 28 before touching app code (same discipline as
  the vitest floor pass — most new failures will be environment, not app).

## Consumer Upgrade Impact

Impact class: `additive` (dev-only) unless suites need environment fixes.

## Validation

- [x] underlay component suite green; consumer suites green or
  failure-proven-environmental with notes

## Stop Conditions

If a suite needs per-test rewrites beyond a small number, stop and
reassess the version target.

## Completion Notes

Completed 2026-08-04. jsdom 30: underlay declares jsdom explicitly at 30.0.1 (5ba60892 — it previously arrived transitively at 29 via isomorphic-dompurify; vitest expects it as a peer). Consumers: acme-admin/cp-admin/dairy/froyo bumped 28 -> 30.1. Test results: underlay 45 component + 758 unit green; acme-admin 32/32, cp-admin 28/28, froyo 9/9 green; dairy 53 failures = exact pre-existing set (A/B-proven on jsdom 28 during the vitest-floor pass), zero new failures on 30.

## Next Task

`g09.020` zod 4.
