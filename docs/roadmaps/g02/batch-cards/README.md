# Batch Cards

Use this folder for `g02` execution cards.

`g02` is closed. These cards are retained here as generation-local history
after the batch-card relocation protocol update.

## Rule

- keep historical `g02` cards here for traceability
- do not treat this folder as a live queue

## Next Task

Leave `g02` closed and use the active generation front doors instead.
