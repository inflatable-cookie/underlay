---
title: Underlay Poodle 0.3.0 adoption worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-for-review
owner: Tom / Northstar orchestrator
created: 2026-09-05
updated: 2026-09-05
handoff_path: /Users/tom/.paseo/worktrees/2shdt2xk/g16-109-underlay-v030-adoption/docs/handoffs/20260905-092656-g16-109-underlay-v030-adoption.md
base_required: pushed-main
tags: [coordination, handoff, worker, dependency-adoption, poodle]
---

## What This Thread Was Doing

Adopt published Poodle Svelte `0.3.0` in Underlay so the rest of the
Underlay-family consumer wave can pin the same registry identity. The lane
moved the exact root pin off `0.2.2`, retargeted the two Nightfire markdown
wrappers to `/markdown`, and absorbed the compiled-package test and
NumberInput type fallout in Underlay's own files.

This is one bounded implementation lane. No transcript or second prompt is
part of the authority chain.

## Why It Matters

Underlay is the Tier 1 foundation for Acowtancy, Compli Me, Contact Patch,
Songsprout, and Underlay Reference. Those app lanes stay gated until this pin
merges. Poodle `0.3.0` drops root markdown-family exports; leaving Underlay on
`0.2.2` would force every downstream Nightfire wrapper to keep a removed
import.

## Current State

Here is the state this lane is handing back:

- **Done:** exact registry pin `@inflatable-cookie/poodle-svelte` `0.3.0`;
  transitive `@inflatable-cookie/poodle-core` `0.3.0`; two markdown wrappers
  import from `/markdown`; NumberInput committed-value coercion in
  `ContextActionDialog.svelte`; component-test Vite `ssr.noExternal` for
  compiled Poodle CSS; jsdom `ResizeObserver` stub for Tabs indicator
  measurement. `effigy qa` is green. Underlay `PAPERCUTS.md` has no
  Poodle-attributed open entries, so none were closed.
- **Still open:** provider review and orchestrator merge. Poodle card
  `g16.109` closeout stays with the Poodle coordinator. Tier 2 Underlay-family
  app lanes stay unlaunched until this PR merges.
- **Repository:** `git@github.com:inflatable-cookie/underlay.git`
- **Planning branch:** `main`
- **Planning base commit:** `7adaaf2e1b138ec0d7fc6dc30a85ec2a19918e5f`
- **Pushed main verification:** `HEAD == origin/main` at
  `7adaaf2e1b138ec0d7fc6dc30a85ec2a19918e5f` before mutation
- **Planning checkout:** clean dedicated worktree; primary
  `/Users/tom/Dev/projects/underlay` left untouched
- **Worker mode:** implementation worker dispatched by the orchestrator
- **Planning artifacts included at the base:** Underlay `v0.9.7` on `main`;
  Poodle card and release note live in the Poodle repo, not this tree
- **Worker branch:** `worker/g16.109-underlay-v030-adoption`
- **Worker worktree:** `/Users/tom/.paseo/worktrees/2shdt2xk/g16-109-underlay-v030-adoption`
- **Worktree creation command:** launcher-provided Paseo `branch-off` from
  pushed `origin/main`
- **Required sibling worktree links:** none
- **Active spec lane:** none in Underlay; Poodle
  `docs/specs/` web-distribution / markdown-boundary notes govern the break
- **Roadmap milestone:** Poodle
  `/Users/tom/Dev/projects/poodle/docs/roadmaps/g16/109-v030-consumer-adoption-wave.md`
- **Ready cards, in order:** that same `g16.109` Underlay row
- **Allowed runway:** Underlay Tier 1 pin only
- **Remaining card budget:** this lane is complete pending review/merge
- **Coordinator agent ID:** current Paseo parent orchestrator; report through
  the child finish notification
- **Delivery route:** coordinator-attached child with `notifyOnFinish: true`
- **Dispatch topology:** serial Underlay foundation; do not launch Acowtancy,
  Compli Me, Contact Patch, Songsprout, or Underlay Reference until this
  merges
- **Parallel safety check:** those five app lanes share this pin and the
  markdown import break; they stay serial behind this merge. Longhorn and
  other Tier 1 Poodle consumers are separate repositories.
- **Surfaces this lane owns:** root `package.json` / `bun.lock`, the two
  Nightfire markdown wrappers, `ContextActionDialog.svelte`, component-test
  Vite config and setup, this handoff
- **Integration ownership:** Poodle coordinator owns card/wave closeout,
  review, and merge; Underlay roadmap front doors are not this lane
- **Canonical refs:** Poodle `docs/release-notes/0.3.0.md`; Underlay
  `AGENTS.md`; `docs/contracts/001-working-rules.md`
- **Review oracle:** Poodle `g16.109` review-oracle table
- **Model capability profile:** ordinary capable implementation worker
- **Worker provider/model identity:** Cursor Grok 4.6 in the launcher worktree
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** registry only; no sibling `file:` / path
  override; no Poodle edits; no desktop/native proofs; no merge; no
  `.github/workflows/` or release mutation
- **Required validation:** `bun --version` `1.3.14`; `effigy --json deps status bun`;
  lock grep; `effigy qa`; `git diff --check`
- **PR base/head:** `main` (`7adaaf2e1b138ec0d7fc6dc30a85ec2a19918e5f`) <-
  `worker/g16.109-underlay-v030-adoption`
- **PR URL:** https://github.com/inflatable-cookie/underlay/pull/26
- **PR commit lineage:** `30c287d78a3758f37d34d3cfe6e7a7427cc4cc9b`
  (adoption: pin, `/markdown` imports, test/type fallout) then
  `80085623637e0589bbd9a5bccf62416cd91dd66b` (recorded the PR URL). The
  first-review provider head was `80085623`. The `30c287d7` pointer in that
  commit was stale: it named the parent, not the tip. This follow-up is the
  SHA-correction revision; a committed tree cannot contain its own hash, so
  re-review the exact PR tip after this push, not `30c287d7`.
- **Review state:** first review blocked on the stale `30c287d7` pointer.
  Re-review the exact current PR head after this push.
- **Merge path:** orchestrator after accepted review of the current head and
  passing required checks
- **Key files:**
  - `/Users/tom/.paseo/worktrees/2shdt2xk/g16-109-underlay-v030-adoption/package.json`
  - `/Users/tom/.paseo/worktrees/2shdt2xk/g16-109-underlay-v030-adoption/bun.lock`
  - `/Users/tom/.paseo/worktrees/2shdt2xk/g16-109-underlay-v030-adoption/ts/src/nightfire/markup/MarkdownEditor.svelte`
  - `/Users/tom/.paseo/worktrees/2shdt2xk/g16-109-underlay-v030-adoption/ts/src/nightfire/markup/MarkdownEditorSurface.svelte`

## Boundaries

Please keep this run inside the named runway:

- **In scope:** exact public-registry Poodle Svelte `0.3.0` pin, lock limited
  to Poodle, the two root markdown imports, and compatibility fallout the bump
  caused in Underlay's own code and headless tests.
- **Out of scope:** Poodle source, sibling path overrides, HistoryCenter (no
  Underlay callers), papercuts not fixed by this release, desktop/native
  proofs, Underlay version bump, consumer-app lanes, merge.
- **Outcome shape:** issue fix. Absorb documented breaks; do not re-export
  removed root names.
- Do not invent architecture, change contracts, or widen the wave.
- Work only in the clean worker worktree. Do not merge the PR.

## Important Context

- **Planning lineage:** Poodle `v0.3.0` tag
  `85609d941a208ff2f854e9f7c0e457089cc77d0e`; main promotion `e3c63e910`;
  card `g16.109`. Underlay previously adopted `0.2.2` from the registry in
  PR #5.
- **How the plan fits the system:** Poodle stays behind Underlay adapters.
  Nightfire wrappers already owned `MarkdownEditor`; they now import the
  `/markdown` subpath the release note requires.
- **Decisions and preferences:** `poodle-core` is transitive, not a new root
  declaration. Compiled CSS imports are handled by Vite `ssr.noExternal`, not
  a Poodle alias. NumberInput `value` is committed `number | null`; string
  field defaults are coerced rather than passed as draft through `value`.
- **Open tensions:** none remaining in this lane. The open Underlay papercut
  about deprecated `effigy docs` routing is unrelated and stays open.
- **Report after:** this PR
- **Report to:** the owning coordinator through the linked child result

## Suggested Next Move

Re-review the exact PR tip after this SHA-correction push against the
`g16.109` oracle. Do not treat `30c287d7` as the review target. Do not merge
and do not start Tier 2 until that re-review accepts the current head.

## Completion Protocol

### Before you start

1. `handoff_mode: worker-pr-loop`, `worker_mode: implementation`, and
   `dispatch_authority: orchestrator` activate worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Accept a clean launcher-provided non-`main` worktree. Do not create another
   because its generated path differs from a planned label.
3. Fetch origin with non-interactive SSH. Require `HEAD == origin/main`,
   require `7adaaf2e1b138ec0d7fc6dc30a85ec2a19918e5f` to be an ancestor, and
   treat the tracked copy of this file as canonical. There are no sibling
   links to create.
4. Read `AGENTS.md`, the Poodle card, `docs/release-notes/0.3.0.md`, and
   Underlay `PAPERCUTS.md`.

### While you work

- Pin from the public registry with this repo's Bun (`1.3.14`). Prove
  `effigy --json deps status bun` has no bun links and the lock has no
  sibling `file:` / `../poodle` source.
- Repair only the two markdown-family root imports plus board fallout in
  Underlay's own files. No Poodle edits.
- Close papercuts only when the release note actually contains the fix.

### When the assigned runway is complete

1. Run `effigy qa` and `git diff --check`.
2. Falsify the oracle: second Poodle version, `file:` source, re-export of a
   removed root name, unrelated lock churn, dishonest papercut close.
3. Push this worker branch and open one PR against current pushed `main`.
4. Report exact base/head, changed files, lock/dependency evidence, tests,
   this absolute handoff path, and the PR URL. Do not merge.

### Review and merge path

The orchestrator launches an independent review child in this worker
workspace under a serial clean exact-head lease. The reviewer uses a
different underlying provider/model identity from the authoring worker.
Current review state: first review requested the handoff name the exact PR
head (`80085623637e0589bbd9a5bccf62416cd91dd66b`) or record the two-commit
relationship; this revision does that. Requested changes after this push:
re-review the new tip.

Merge stays with the orchestrator after accepted review of the current head
and passing required checks. Do not launch Acowtancy, Compli Me, Contact
Patch, Songsprout, or Underlay Reference until this foundation merges.

- **Closeout refs:** Poodle `g16.109` and wave closeout are coordinator-owned;
  this handoff is the Underlay lane record.
