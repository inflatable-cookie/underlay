---
title: Underlay v0.9.7 release execution
kind: release-handoff
handoff_mode: release-execution
dispatch_authority: orchestrator
status: ready-to-launch
owner: Tom / Northstar orchestrator
created: 2026-09-02
updated: 2026-09-02
handoff_path: /Users/tom/Dev/projects/underlay/docs/handoffs/20260902-230500-underlay-v0-9-7-release.md
base_required: pushed-main
tags: [coordination, release, underlay, g11]
---

## What This Thread Owns

Execute `g11.001` Card 004 only: validate and publish Underlay `v0.9.7` from
the exact clean pushed candidate containing this handoff. The operator has
explicitly authorized the primitive-and-release lane. This is a release
operator on primary `main`, not an implementation worker or PR loop.

## Current State

- Repository: `/Users/tom/Dev/projects/underlay`
- Planning base before closeout: `c8378e6bd9372a8319fdc2f114a84e504cce8537`
- Candidate: the exact pushed `main` commit containing this handoff and Card
  003 closeout; record it after preflight and never substitute a newer SHA
- Card 003: accepted and merged as PR #25
- Card 004: ready and explicitly authorized
- Target: one immutable annotated `v0.9.7` tag and published GitHub Release
- Consumer changes: none; consumers remain separate follow-up lanes
- Merge/PR: none; release tooling owns the synchronized release commit

## Required Sequence

Read `AGENTS.md`, `PAPERCUTS.md`, Card 004, Contracts 023/040/122, the active
spec, the v0.9.6 release log, and Effigy's release protocol. Then:

1. Fetch tags/prune. Require clean `main`, `HEAD == origin/main`, Card 003 merge
   ancestry, and absence of `v0.9.7` locally and remotely. Record candidate SHA.
2. Require fresh hosted CI whose `headSha` equals that candidate. This repo has
   `rust.yml` without `workflow_dispatch`, not `ci.yml`; use the exact-SHA push
   run as evidence and do not claim the unavailable canonical dispatch ran.
3. Run `effigy release simulate`, `effigy release status --check-gates`, and
   `effigy release prepare --plan`. Stop on any blocker or unexpected mutation.
4. Before the immutable tag, finalize CHANGELOG link references so the tagged
   `[0.9.7]` section and `[Unreleased]` comparison are correct. The v0.9.6 tag
   missed this; do not repeat it.
5. Run `effigy release prepare --yes --check-gates`, inspect the complete drift,
   and require only synchronized `0.9.7` release surfaces plus correct changelog
   promotion/link references. Run diff hygiene.
6. Run `effigy release execute --plan`; require prepared/current identity to
   match. Then run the explicitly authorized `effigy release execute --yes`
   exactly once. Never retag or retry around a partial failure.
7. Publish and verify the GitHub Release for `v0.9.7` if Effigy does not. Do not
   add or change workflows.
8. Validate the annotated tag directly with Git/GitHub API. Do not use
   `effigy release validate --tag` or `verify-install`: v0.9.6 proved those are
   Effigy-binary/self-hosting routes and invalid for this library repo.
9. Compile a throwaway clean Cargo consumer from tag `v0.9.7`, calling the
   owned promotion/recovery public surface through generic bounds. Require its
   lock source to be the tag and exact release commit—no branch, revision, or
   path. Remove the throwaway consumer.
10. Require hosted CI green on the release commit, extract the changelog, run
    docs/Northstar QA and diff checks, then record Card 004/release closeout on
    `main` without rewriting the tag.

## Stop Conditions

- Any configured release gate, exact-SHA CI, version-sync, changelog, tag,
  consumer-smoke, or repository-identity check fails.
- Candidate contains unrelated drift, `v0.9.7` already exists, or prepared
  state becomes stale.
- A step would require bypassing a gate, editing `.github/workflows/`, changing
  a consumer, using a local/path/unreleased dependency, retagging, or hiding a
  partial release.
- If release execution partially succeeds, stop and report exact remote/local
  state. Do not retry, delete, or recreate the tag.

## Boundaries

No consumer edits, implementation changes, PR, workflow edit, force push,
retag, bypass, or parallel release. Do not expose secrets or provider error
payloads. Preserve the primary checkout if it is dirty or moved; report rather
than cleaning, stashing, resetting, or selecting a different candidate.

## Evidence To Return

Candidate SHA; exact-SHA CI URL/result; every gate; prepare/execute plans;
release commit; annotated tag object and peeled commit; GitHub Release URL;
tagged consumer lock/source proof; changelog extraction; closeout commit;
`origin/main`/local identity; clean-tree proof; and any honest deviations.

## Next Task

After a successful release, return to the orchestrator. It will resume
Underlay Reference first and route the other consumer ownership upgrades as
independent repository lanes.
