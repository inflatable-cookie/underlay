---
title: g13.001 Poodle 0.4.1 producer release worker
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
status: ready-to-launch
owner: repo maintainers
created: 2026-09-14
updated: 2026-09-14
base_required: pushed-main
roadmap: docs/roadmaps/g13/001-poodle-0-4-1-producer-release.md
queue_dispatch: northstar-queue
queue_approval: "Operator-confirmed direction in this thread on 2026-09-14: promote and execute the Underlay Poodle 0.4.1 producer release lane for the blocked Market adoption."
tags: [coordination, handoff, worker, dependency-adoption, poodle]
---

## What This Thread Was Doing

Pin published Poodle `0.4.1` in Underlay and prepare the additive
`v0.9.9` producer release that Acowtancy Market `g05.070` is blocked
on. The lane moves the one committed root pin off `0.3.0`, regenerates
the root lock, absorbs only forced compatibility fallout, and stages
the synchronous version/changelog files for review. Tag creation and
merge stay outside the worker.

This is one bounded implementation lane. No transcript or second prompt
is part of the authority chain.

## Why It Matters

Market's blocked handoff
`20260914-095635-g05-070-poodle-0-4-1-consumer-adoption.md` cannot go
to Queue until an accepted Underlay tag exists whose package metadata
declares Poodle `0.4.1`. Market owns only its three manifests plus
root lock and must not touch Underlay, so this producer side has to
move first. The `0.3.0` → `0.4.1` jump also crosses Poodle's `0.4.0`
breaking minor, which is why the task fronts the caller census instead
of a blind bump.

## Current State

Here is the state this lane hands over:

- **Done (planning):** `g13.001`
  (`docs/roadmaps/g13/001-poodle-0-4-1-producer-release.md`) is
  `ready` with the full census baseline, dispatch manifest, oracle,
  and stop conditions. New parallel generation `g13` is independent of
  `g11.002` and `g12.001`/Card 278. The triage blocker is retired by
  this promotion.
- **Still open:** the worker PR (steps 1–7 of the task), then
  orchestrator review/merge and the `v0.9.9` tag as task closeout
  (step 8).
- **Repository:** `git@github.com:inflatable-cookie/underlay.git`
- **Planning branch:** `main`
- **Planning base commit:** `69680c61`
- **Pushed main verification:** `HEAD` equalled `origin/main` at
  `69680c61` before this promotion; this handoff is committed after
  that base — require it as an ancestor and treat the tracked `HEAD`
  copy as canonical.
- **Planning checkout:** `/Users/tom/Dev/projects/underlay` on
  `main`; do not execute work here — the worker uses its own clean
  worktree.
- **Worker mode:** implementation worker dispatched by the
  orchestrator; this handoff activates the worker-only worktree
  preflight.
- **Planning artifacts included at the base:** `g13/README.md`,
  `g13.001`, updated `docs/roadmaps/README.md` and
  `docs/roadmaps/generation-index.md`; Poodle release evidence lives
  in the Poodle repo (`docs/release-notes/0.4.1.md`, `CHANGELOG.md`
  `0.4.0` breaking section), not this tree.
- **Worker branch:** `worker/g13-001-poodle-0-4-1-producer`
- **Worker worktree:** launcher-provided Paseo `branch-off` from
  pushed `origin/main`; do not create another because a generated
  path differs from a planned label.
- **Required sibling worktree links:** none.
- **Active spec lane:** none; consumer adoption of a published
  release against the task's falsifiable census.
- **Roadmap milestone:** Underlay `g13.001`.
- **Ready cards, in order:** that same `g13.001`.
- **Allowed runway:** root `package.json` / `bun.lock`, forced
  fallout in owned files (two Nightfire markdown wrappers;
  component-test Vite config/setup only if fallout proves),
  synchronous version files (`Cargo.toml`
  `workspace.package.version`, `package.json` version,
  `Cargo.lock`, `CHANGELOG.md` plus upgrade-note block), this
  handoff.
- **Remaining card budget:** this one producer lane.
- **Coordinator agent ID:** owning orchestrator; report through the
  child finish notification.
- **Delivery route:** one PR against Underlay `main`; merge and tag
  stay with the orchestrator/maintainers after accepted review.
- **Dispatch topology:** parallel with `g11.002` and
  `g12.001`/Card 278 (no shared paths, no ordering). Market
  `g05.070` is serial behind the produced tag and unblocks itself.
  Longhorn is a parallel producer lane with no edge in either
  direction.
- **Parallel safety check:** media and Nightfire lanes never touch
  the Poodle pin or the release files; this lane never touches media
  or Nightfire semantics. Longhorn lives in a separate repository.
- **Surfaces this lane owns:** the allowed runway above plus this
  handoff.
- **Integration ownership:** Underlay roadmap closeout and the
  `v0.9.9` tag stay with the orchestrator/maintainers; Market
  unblocking stays with Market; Poodle wave closeout (if any) stays
  with the Poodle coordinator.
- **Canonical refs:** `docs/contracts/001-working-rules.md`,
  `docs/contracts/023-release-and-compatibility-rollout.md`,
  Poodle `docs/release-notes/0.4.1.md`; Underlay `AGENTS.md`.
- **Review oracle:** the `g13.001` acceptance table (exact graph,
  transitive core, safe markdown, scoped lock, frozen reproducible,
  synchronous release, shipped upgrade notes, no Longhorn edge).
- **Model capability profile:** ordinary implementation.
- **Worker provider/model identity:** orchestrator-routed; record the
  actual identity in the closeout.
- **Frontier-worker justification:** none.
- **Tool/runtime restrictions:** public registry only with this
  repo's Bun (`1.3.14`); no sibling Poodle path, `file:`, or local
  override; no Poodle/Longhorn/consumer edits; no overrides or
  hand-edited locks; no desktop/native/windowed proofs; no merge, no
  tag creation, no `.github/workflows/` edits.
- **Required validation:** `bun install` clean; frozen rerun
  no-diff; `effigy --json deps status bun`; lock grep for one Poodle
  identity and zero `0.3.0` Poodle records;
  `check:release-version-sync`; `effigy validate`; `rust:check`;
  `rust:clippy`; `git diff --check`.
- **PR base/head:** `main` ←
  `worker/g13-001-poodle-0-4-1-producer`.
- **PR URL:** pending.
- **Review state:** awaiting worker PR.
- **Merge path:** orchestrator after accepted review of the current
  head and passing required checks; tag `v0.9.9` follows as task
  closeout.
- **Key files:**
  - `/Users/tom/Dev/projects/underlay/docs/roadmaps/g13/001-poodle-0-4-1-producer-release.md`
  - `/Users/tom/Dev/projects/underlay/package.json`
  - `/Users/tom/Dev/projects/underlay/bun.lock`
  - `/Users/tom/Dev/projects/underlay/ts/src/nightfire/markup/MarkdownEditor.svelte`
  - `/Users/tom/Dev/projects/underlay/ts/src/nightfire/markup/MarkdownEditorSurface.svelte`

## Boundaries

Please keep this run inside the named runway:

- **In scope:** the census re-verification, the exact public-registry
  `poodle-svelte` `0.4.1` pin, root lock regeneration with
  before/after accounting, forced fallout in owned files only,
  reviewable `0.9.9` version/changelog edits inside the PR, and this
  handoff.
- **Out of scope:** product semantics, consumer repositories, Poodle
  source, Longhorn in any form, historical docs (`0.2.2` examples,
  `contracts/ui/*poodle*` snapshots), sibling path resolution,
  overrides, hand-edited locks, desktop or native proofs, merge, tag
  creation, release mutation beyond the PR's file edits.
- **Outcome shape:** complete pin-and-stage lane. Open a reviewable
  PR; do not merge and do not cut the tag.
- Work only in the launcher worktree. Do not edit the primary
  checkout.
- Stop if the census is falsified, lock churn is unrelated, public
  registry resolution fails, a breaking caller impact appears, or any
  release gate stays red. Send a complete blocker capsule; do not ask
  the operator directly or open a placeholder PR.

## Important Context

- **Planning lineage:** operator directive to move every Poodle pin
  to published `0.4.1`; Acowtancy `g05.070` task plus its blocked
  worker handoff; Poodle `0.4.1` release note and `0.4.0` breaking
  changelog; Underlay `0.3.0` adoption precedent (PR #26) where core
  stayed transitive and markdown wrappers moved to `/markdown`.
- **How the plan fits the system:** Underlay stays the Tier 1
  foundation — one truthful producer manifest plus lock, additive
  patch release, reference-first rollout per contract 023. Nightfire
  wrappers keep owning Markdown; they now prove the safe-by-default
  path instead of changing it.
- **Decisions and preferences:** `poodle-core` is transitive, not a
  new root declaration. Preview sanitization stays in Underlay's
  helper; no `htmlPolicy="trusted"`. Patch `v0.9.9` follows the
  census (no removed-surface use); a falsified census returns to
  planning for the minor-bump decision. Longhorn is parallel with no
  dependency — never wait on it.
- **Open tensions:** the new transitive CodeMirror/TipTap graph is
  unmeasured until the worker's lock report; the `v0.9.9` number is
  planned, not cut — the tag is closeout, not worker output.
- **Report after:** one PR with census log, manifest/lock evidence,
  validation runs, and this handoff's absolute path.
- **Report to:** the owning orchestrator through the linked child
  result. Do not merge.

### UI Design Brief

Not applicable — this lane is not UI work.

## Suggested Next Move

Reuse the launcher worktree. Run the preflight probes, confirm the
planning base is an ancestor and the tracked handoff matches, then
work `g13.001` steps 1–7 in order: census, pin, lock with accounting,
fallout, version/changelog staging, full validation, PR. Leave steps
8 (merge, tag, Market handoff of the triple) to the orchestrator.

## Completion Protocol

### Before you start

1. This handoff's `worker_mode: implementation` and
   `dispatch_authority: orchestrator` activate worker mode. Before
   broad reads, run `git rev-parse --show-toplevel`,
   `git branch --show-current`, `git status --porcelain`, and
   `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty,
   and its branch is not `main`, accept it as the launcher-provided
   worktree. Record its actual root/branch; do not create another
   worktree.
3. From the selected worktree, record this handoff's
   repository-relative path
   (`docs/handoffs/20260914-124500-g13-001-poodle-0-4-1-producer.md`).
   Run `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Confirm the planning base `69680c61` is an ancestor of `HEAD`.
   After this file is committed, the tracked `HEAD` copy is canonical.
4. Required sibling worktree links are `none`; skip link creation.
5. Read `AGENTS.md`, `g13.001`, contracts 001/023, and the Poodle
   `0.4.1` release note plus `0.4.0` breaking section.
6. Run cheap orientation (`effigy tasks`, `effigy doctor`) and record
   it.

### While you work

- Keep commits aligned with one adoption chunk, not model turns.
- Pin from the public registry with this repo's Bun. Prove
  `effigy --json deps status bun` has no bun links and the lock has
  no sibling `file:` / `../poodle` source.
- Repair only forced fallout in owned files. No Poodle edits, no
  Longhorn or consumer edits.
- Stop before PR if the census is falsified, intent is ambiguous, or
  validation changes the plan. Send a complete blocker capsule; do
  not ask the operator directly or open a placeholder PR.
- Do not invent architecture or shim removed Poodle exports.

### When the assigned runway is complete

1. Run the full task validation and `git diff --check`. Prove one
   Poodle identity from public registry resolution plus the frozen
   rerun.
2. Falsify the diff against the task's review oracle.
3. Push `worker/g13-001-poodle-0-4-1-producer` and open one PR
   against current `main`.
4. Return the PR URL, changed files, census log, lock evidence,
   tests, and this handoff's absolute path. Do not merge and do not
   cut the tag.

### Review and merge path

Independent review of the exact PR head. Merge belongs to the
orchestrator after accepted review and passing checks; tag `v0.9.9`
and the Market triple follow as `g13.001` step 8.

- **Closeout refs:** `g13.001` Delivery, `g13/README.md` frontier,
  Poodle `0.4.1` evidence as cited. No Poodle-side closeout is this
  lane's.

### Handoff closeout

Leave the PR honest about residual historical `0.3.0`/`0.2.2`
mentions that are docs-only and any validation baseline inherited at
the exact worker base.
