# 2026-08-27 12:41:17 - g09.048-052 Consumer Promotion

## Outcome

All five independent consumer runtime/access lanes are ready. Each target was
clean and exactly aligned with `origin/main`; the Underlay Reference proof is
merged; and the remaining product/security decisions are recorded in the
numbered roadmaps.

## Exact-Main Evidence

| Roadmap | Consumer | Verified head |
| --- | --- | --- |
| `g09.048` | Contact Patch | `8d5b6f4c463eb4bcdef4e2c60fb16d4cc878c8df` |
| `g09.049` | Compli Me | `12fa0d17cc8abe3c6a15cd7b3e2df352bb7e7f29` |
| `g09.050` | Songsprout | `e1fd46ef1230492dc2be0b5787768350823da5c4` |
| `g09.051` | Composer | `b7cafd9cb281f46ec4ade802eb49b01e1f9b58d8` |
| `g09.052` | Acowtancy | `7c592aab7f51d140debbaabc69793cc0aace46c2` |

## Settled Decisions

- Adopt the Underlay Reference deployed config, cookie, required-secret, and
  cookie-backed mutation CSRF baseline where applicable.
- Make Songsprout rate-limit backend failure fail closed.
- Keep Contact Patch Book external and read-only; deployment edge/abuse
  controls own its cacheable public GET rate posture.
- Keep `/v1/compli/*` shared, remove unexposed Compli metrics wiring, and remove
  unimplemented Songsprout readiness claims.
- Keep Composer socket peer as authoritative until a concrete trusted-proxy
  topology is declared.
- Move Composer delete-batch restore/purge atomically under `/v1/admin/*`; no
  in-repo caller requires a compatibility alias.
- Reconcile Acowtancy secret requiredness with actual startup and selected
  integration behavior rather than treating every injection target as globally
  mandatory.

## Queue Reconciliation

- `g09.048`-`g09.052` are `ready` and may run in parallel as independent
  repo-owned lanes.
- Each lane requires one target-owned handoff committed to the exact main base.
- `g09.053` remains planned behind reviewed merges from all five lanes.

## Next Task

Prepare, validate, commit, and push one worker handoff in each consumer root,
then dispatch the five independent lanes.
