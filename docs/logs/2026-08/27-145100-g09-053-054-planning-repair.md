# 2026-08-27 14:51:00 BST - g09.053-g09.054 Planning Repair

## Outcome

Promoted the Underlay Reference cross-tab CSRF finding into ready roadmap
`g09.053`. Renumbered the unstarted fleet closeout from `g09.053` to `g09.054`
so the numbered queue matches execution order.

## Authority Repair

- `g09.053` owns the bounded Reference implementation and two-tab regression.
- `g09.054` depends on `g09.053` plus the five completed consumer rollouts.
- Historical logs retain the former `g09.053` closeout wording as the planning
  state recorded at their timestamps. This log records the renumber boundary.
- The open triage note was promoted into the numbered roadmap and removed.

## Settled Boundary

Reuse a non-empty CSRF cookie on token GET; mint only when absent or empty. Keep
the double-submit model stateless and preserve the route, response envelope,
cookie attributes, and client API. Prove that tab B's read leaves tab A's
cookie/header mutation valid through the actual middleware.

## Next Task

Publish a target-owned Underlay Reference handoff for `g09.053`, dispatch one
serial worker PR, then promote `g09.054` only after reviewed merge evidence.
